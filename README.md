# Excel-File-Sharing-Authorization-System
A secure web application that allows users to upload Excel files, automatically convert them into read-only web tables, and manage file permissions using a role-based access system.

This project was built using Python, Django, and PostgreSQL.
